ssmonidaacountsapi.bizscal.com
sudo nano /etc/nginx/sites-available/ssmonidaacountsapi.bizscal.com
sudo ln -s /etc/nginx/sites-available/ssmonidaacountsapi.bizscal.com /etc/nginx/sites-enabled/
server {
    listen 443 ssl;
    http2 on;
    server_name ssmonidaacountsapi.bizscal.com;

    client_max_body_size 20M;

      # --- SSL Certificate Configuration ---
    # Your certificate and private key paths
    ssl_certificate /etc/letsencrypt/live/bizscal.com-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bizscal.com-0001/privkey.pem;

    # Recommended security settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:ECDHE-RSA-AES128-GCM-SHA256';
    ssl_prefer_server_ciphers off;

    location / {
        # Change 3000 to your Node.js app's port number
        proxy_pass http://localhost:5055;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo nginx -t
sudo systemctl reload nginx

ssmonidaacounts.bizscal.com
sudo nano /etc/nginx/sites-available/ssmonidaacounts.bizscal.com
sudo ln -s /etc/nginx/sites-available/ssmonidaacounts.bizscal.com /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name ssmonidaacounts.bizscal.com www.ssmonidaacounts.bizscal.com;

    if ($host = ssmonidaacounts.bizscal.com) {
        return 301 https://$host$request_uri;
    }

    return 301 https://$host$request_uri;


}

# This block serves your frontend application securely over HTTPS
server {
    listen [::]:443 ssl;
    listen 443 ssl;

    server_name ssmonidaacounts.bizscal.com www.ssmonidaacounts.bizscal.com;

    # Enable HTTP2 the modern way
    http2 on;

       # --- SSL Certificate Configuration ---
    # Your certificate and private key paths
    ssl_certificate /etc/letsencrypt/live/bizscal.com-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bizscal.com-0001/privkey.pem;

    # Recommended security settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:ECDHE-RSA-AES128-GCM-SHA256';
    ssl_prefer_server_ciphers off;

    root /var/www/ssmonidaacounts.bizscal.com;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

}

sudo nginx -t
sudo systemctl reload nginx

